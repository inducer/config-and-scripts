#! /bin/bash

if ! command -v delta &> /dev/null; then
    exec less "$@"
else
    if ! test "$TERMINAL_LIGHTNESS" == ""; then
        exec delta "--$TERMINAL_LIGHTNESS" "$@"
    else
        exec delta "$@"
    fi
fi
